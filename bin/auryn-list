#!/usr/bin/env node

/*
 * List env items (libs / sources)
 */


/**
 * Module dependencies.
 */

var auryn = require('../package.json');

var program = require('commander');

program
  .version(auryn.version)
  .command('sources', 'list sources to generate')
  .parse(process.argv);

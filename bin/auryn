#!/usr/bin/env node

/*
 * Generate Docks (Android or iOS)
 */


/**
 * Module dependencies.
 */

var auryn = require('../package.json');

var program = require('commander');

program
  .version(auryn.version)
  .command('dock', 'dock a project (android or iOS)')
  .parse(process.argv);
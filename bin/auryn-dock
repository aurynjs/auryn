#!/usr/bin/env node

/*
 * Generate Docks (Android or iOS)
 */


/**
 * Module dependencies.
 */

var auryn = require('../package.json');

var program = require('commander');

program
  .version(auryn.version)
  .command('android', 'dock an android project')
  .parse(process.argv);